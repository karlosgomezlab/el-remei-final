{
  "name": "el-remei-sgi-poligono",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/client -- --port 3101\" \"npm run dev --workspace=apps/admin -- --port 3100\" \"npm run dev --workspace=apps/landing -- --port 3002\"",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start --workspaces --if-present"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "concurrently": "^9.2.1",
    "supabase": "^2.75.3"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "dotenv": "^17.2.4"
  },
  "overrides": {
    "@types/react": "18.2.0",
    "@types/react-dom": "18.2.0"
  }
}